# aria2
aria2 is a bash wrapper using aria2c to download files whilst also storing defaults. It can also use wget to crawl a URL folder and find all the filenames as well as structure before aria2c downloads it. It supports multiple flags, compression etc. It "remembers" things like how many connections you want, whether you want compressed, etc whilst keeping URL and filepath temporary.
